# 🧱 AI Template Project Context

Этот проект `ai-template` — базовый шаблон для создания продакшн-готовых AI‑ботов и сервисов на Python.

## 🎯 Цель шаблона
- Дать стандартный скелет проекта: структура директорий, конфиги, Makefile, docker-compose.
- Упаковать лучшие практики: `asyncio`, Pydantic Settings, `loguru`, загрузка `.env`.
- Обеспечить быстрый старт для Telegram-ботов и других AI-сервисов без копипаста.

## 📂 Ключевая структура
- `src/main.py` — минимальная async-точка входа (Telegram bot skeleton).
- `src/config.py` — конфигурация через `pydantic-settings.BaseSettings` и `.env`.
- `Makefile` — единая точка управления (install, run, docker-up/down, clean).
- `docker-compose.yml` — базовый сервис `bot` для локального/dev окружения.

## 🔐 Секреты и окружения
- Все ключи и токены берутся только из `.env`.
- В репозитории хранится только `.env.example` без реальных значений.
- Переменные по умолчанию:
  - `TELEGRAM_TOKEN`
  - `OPENAI_API_KEY`
  - `ENVIRONMENT` (по умолчанию `local`)

## 🚀 Типичный флоу разработки
1. Скопировать шаблон `ai-template` под новый проект.
2. Создать `.venv`, установить зависимости: `make install`.
3. Настроить `.env` на основе `.env.example`.
4. Запустить локально: `make run` или через Docker: `docker-compose up bot`.

